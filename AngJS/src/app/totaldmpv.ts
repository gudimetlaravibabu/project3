export class Totaldmpv {
    
    constructor(public totalphcTarget : number, public totalvolTarget : number, public totalphcAOD : number, public totalvolAOD : number, public totalphcAOL : number, public totalphcGap : string, public totalvolGap : string)
        {
    
        }
}
